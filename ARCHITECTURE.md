# Architecture

This repository is structured as a workspace with isolated crates for core domain logic, networking, decoding, storage, UI, platform abstractions, and optional extension points.

See `README.md` for the product framing and the `docs/` book for user-facing and contributor-facing details.
